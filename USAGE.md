# 音符 Note · 使用说明

> 给最终用户的安装与使用指南。如果你刚下载了 APK，这是你唯一需要看的文档。

## 📱 安装

### 系统要求
- Android 5.0+（API 21），已在 minSdk=24 / targetSdk=33 编译
- ARM 64 位（绝大多数 2017 年后手机）
- 联网（音乐搜索/解析/播放都需要）

### 步骤
1. 把 `音符-note-vX.X.X-debug.apk` 传到手机（USB / 微信文件传输 / 网盘均可）
2. 手机上点开 APK，第一次会被询问"允许安装未知来源应用"，**同意**
3. 装好后在桌面找"音符"图标点开
4. 首次启动会请求通知权限（用于通知栏控制），**允许**才有后台控制和锁屏控制

> Debug 包没有 Google Play 签名，**部分手机**会提示"风险"——本项目全部代码开源，无后门，可以放心。

## 🎵 主页能做什么

打开应用默认进入「**首页**」Tab。布局借鉴 lx-music-mobile / 网易云音乐：

```
┌─────────────────────────────────┐
│  横幅                          │  装饰，可忽略
├─────────────────────────────────┤
│  推荐歌单    每日更新 →          │
│  [歌单] [歌单] [歌单] ... →     │  ← 横向滚动，点开进歌单详情
├─────────────────────────────────┤
│  新歌速递    为你推荐 →          │
│  [小卡] [小卡] [小卡] ... →     │  ← 点开直接播放
├─────────────────────────────────┤
│  排行榜      热歌榜 →            │
│  [榜1] [榜2] [榜3] [榜4] →      │  ← 点开进榜单详情
└─────────────────────────────────┘
```

- **数据来源**：NetEase 公开 API（不需要登录），手机端直接拉
- **歌单详情**：点歌单卡 → 真实加载该歌单全部歌曲（一般 50-100 首）→ 点歌播放
- **排行榜详情**：点榜单卡 → 加载榜单全部歌曲
- **新歌速递**：点小卡 → 立即播放单首

> 如果三个 Rail 都显示"首页数据加载失败，请检查网络"，说明你的网络到 NetEase（music.163.com）不通。**搜索功能仍然可用**——它走的是各源自己的接口。

## 🔍 搜索

切到「**搜索**」Tab：

1. **顶部芯片**：勾选/取消要启用的音源
2. **右侧"音源管理"**：弹窗里看每个源的**真实能力**（"可搜可播"/"仅试听片段"/"仅搜索·不可播"/"需配置账号"）
3. **输入关键词**：去抖 300ms 自动搜索，结果**流式追加**
4. **点歌**：立即播放，PlayerRepository 会用歌曲自身的音源尝试解析；失败会用其他已启用源搜"同名歌"补位
5. **点下载按钮**：下载到 `Music/Soundtrack/` 目录

### 哪些源能搜、哪些能播？

| 标签 | 含义 | 例子 |
|---|---|---|
| ✅ 可搜可播 | 完整支持 | 咪咕、网易云、酷我、QQ、酷狗、千千、MusicDL 系列 |
| 🎧 仅试听片段 | 平台只给 30 秒预览 | Apple Music、Deezer |
| 🔍 仅搜索 · 不可播 | 能搜到但拿不到可播地址 | 5 个夸克网盘源、蜻蜓FM、YouTube Music |
| 🔐 需配置账号 | 没账号凭据时不可用 | Spotify、TIDAL、Qobuz、Suno |

> 搜索结果**只展示能播的**（可搜可播 + 仅试听），自动过滤掉"仅搜索"和"需配置"。

## ▶️ 播放

点任意一首歌 → 进入**沉浸式播放页**：

- 顶部大封面（圆角+背景模糊）
- 歌名 / 歌手 / 专辑
- 进度条（可拖动跳转）
- 上一首 / 播放暂停 / 下一首
- 歌词面板（自动滚动到当前行，**长按复制**全部歌词）
- 底部下载按钮

控制方式：
- **通知栏**：后台也能控
- **锁屏控制**：开通知权限后支持
- **蓝牙耳机按键**：支持

## 📥 下载

- 在列表里点歌的"更多"按钮（item_song.xml 里那个**下载图标**）
- 或在播放页点底部下载按钮
- 文件保存到：`/sdcard/Music/Soundtrack/<歌名>-<歌手>.<ext>`
- 自动通过 MediaStore 入库，**文件管理器 / 系统音乐播放器都能看到**

> 下载需要先能解析到播放地址。**首次点歌时解析一次**就够；之后下载/再听都顺畅。

## 👤 我的 Tab

- 已下载列表（按时间倒序）
- **清空缓存**（图片磁盘缓存，不影响下载）
- **音源适配进度**（弹窗显示 57 个源的真实状态 + 4 档能力分组）

## ⚙️ 调试 / 进阶

### 启用开发者日志

BuildConfig 暂未暴露日志开关。直接看 `logcat` 过滤 `com.soundtrack.music`。

### 自己换音源接口

多数公开源接口可能改版。`android/app/src/main/java/com/soundtrack/music/source/` 下每个源都有明确的头注释说明限制；改完重新编译即可。

### 写自己的源

```kotlin
class MySource : MusicSource {
    override val id = "myid"
    override val label = "我的源"
    override suspend fun search(keyword: String, pageSize: Int) = ...
    override suspend fun resolvePlayUrl(song: Song) = ...
    override suspend fun fetchLyric(song: Song) = ...
}
```

然后在 `BuiltinSources.ALL` 加 `Meta("myid", "我的源", "core", native = true, cap = Cap.FULL)`，并在 `SourceResolver.nativeSources` 末尾加 `MySource()`。

## 🆘 常见问题

**Q：为什么搜不到某些歌？**
A：每个源对搜索结果的去重、版权过滤不同。换一个源试试（顶部芯片切换）。

**Q：搜索到了但显示"无法解析《xxx》的播放地址"？**
A：那个源在那一时间点拿不到播放地址（接口失败、限流、版权等）。`PlayerRepository` 已经自动用其他已启用源搜同名歌补位；**如果全部都失败**，就是这首歌当前真的没法在公开源里听。

**Q：能搜到 YouTube 但点开没声音？**
A：YouTube Music 的播放链接需要 yt-dlp 级 JS 反混淆，移动端做不了。搜索能展示，但播放需要付费或客户端。这是**已知限制**（v1.8.0）。

**Q：5 个"仅搜索"的源什么时候能播？**
A：它们（音乐岛、下歌吧、KKWS、栗子YY、FiveSong）的音频全存在**夸克网盘**上，需要夸克账号 cookie + QuarkParser 才能解析。这是一项独立工作，没有时间表。

**Q：通知栏不显示控制？**
A：Android 13+ 需要**手动授予通知权限**。系统设置 → 应用 → 音符 → 通知 → 允许。

**Q：歌词不显示？**
A：只有源提供歌词接口才有。酷狗/网易云/QQ 都有；其他源可能没歌词。`Song.lrc` 为空时显示"暂无歌词"。

**Q：可以同时下载多首吗？**
A：当前是单条串行下载（避免被源限流）。后台会保持下载进度。

**Q：能换主题/暗色模式吗？**
A：当前是固定的深色主题。Material Components 主题切换是未来工作。

**Q：会不会收集我的数据？**
A：不会。本项目**不连接任何自有后端**。所有 HTTP 请求都直连各音乐平台的公开 API。

## 📞 反馈

提 Issue 时附：
1. **操作步骤**（从打开 App 到出问题的完整流程）
2. **logcat 输出**（过滤 `com.soundtrack.music`，从搜索/播放到崩溃）
3. **失败的歌曲 ID**（如可获取）

## 🔗 关联链接

- [README](README.md) — 总体介绍、技术栈
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — 模块划分、关键流程
- [docs/source-porting-report.md](docs/source-porting-report.md) — 音源移植明细
